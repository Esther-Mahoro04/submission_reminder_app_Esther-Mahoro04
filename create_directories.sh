#!/bin/bash
	
user_name="Esther"

main_directory="submission_reminder_${user_name}"

mkdir -p "$main_directory/assets" "$main_directory/config" "$main_directory/modules"

echo "Directories for $main_directory created."
